# ============================================================
#  NightOps — Blue/Green Deployment Configuration
#  נטען ע"י כל הסקריפטים
# ============================================================

# ── Slots ────────────────────────────────────────────────────
BLUE_BACKEND_PORT=3010
BLUE_FRONTEND_PORT=3011
GREEN_BACKEND_PORT=3012
GREEN_FRONTEND_PORT=3013

# ── Paths ────────────────────────────────────────────────────
DEPLOY_BASE=/opt/nightops
STATE_FILE=/opt/nightops/state/active_slot
BACKUP_DIR=/opt/nightops/backups
LOG_DIR=/opt/nightops/logs
NGINX_UPSTREAM_CONF=/etc/nginx/conf.d/nightops-upstream.conf
NGINX_SITE_CONF=/etc/nginx/conf.d/nightops.conf

# ── Timing ───────────────────────────────────────────────────
HEALTH_RETRIES=12        # כמה פעמים לנסות health check
HEALTH_INTERVAL=5        # שניות בין ניסיונות
GRACE_PERIOD=120         # שניות שהגרסה הישנה נשמרת אחרי switch

# ── Backup retention ─────────────────────────────────────────
BACKUP_KEEP_DAYS=14

# ── Repo ─────────────────────────────────────────────────────
GIT_REPO=https://github.com/Nissim2024/night-ops-system-clean2.git
