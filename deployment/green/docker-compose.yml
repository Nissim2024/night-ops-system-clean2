services:

  backend-green:
    image: DeployCenter-backend:${APP_VERSION:-latest}-green
    container_name: DeployCenter-backend-green
    restart: unless-stopped
    env_file: .env.green
    environment:
      PORT: "3012"
      NODE_ENV: production
      SLOT: green
    ports:
      - "127.0.0.1:3012:3012"
    networks:
      - DeployCenter-net
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3012/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  frontend-green:
    image: DeployCenter-frontend:${APP_VERSION:-latest}
    container_name: DeployCenter-frontend-green
    restart: unless-stopped
    ports:
      - "127.0.0.1:3013:80"
    networks:
      - DeployCenter-net
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:80"]
      interval: 10s
      timeout: 5s
      retries: 3

networks:
  DeployCenter-net:
    external: true
    name: DeployCenter-net
