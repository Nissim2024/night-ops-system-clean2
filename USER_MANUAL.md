# Night-Ops System (DeployCenter) — מדריך משתמש

> גרסה: 1.0 | תאריך: מאי 2026

---

## תוכן עניינים

1. [מבוא ותפקידים](#1-מבוא-ותפקידים)
2. [כניסה למערכת](#2-כניסה-למערכת)
3. [מדריך מנהל לילה (RELEASE_MANAGER)](#3-מדריך-מנהל-לילה)
4. [מדריך ראש צוות (TEAM_LEAD)](#4-מדריך-ראש-צוות)
5. [מדריך עובד (EMPLOYEE)](#5-מדריך-עובד)
6. [מדריך מנהל מערכת (ADMIN)](#6-מדריך-מנהל-מערכת)
7. [מחזור חיים של גרסה — מדריך מלא](#7-מחזור-חיים-של-גרסה)
8. [חדר מלחמה (War Room)](#8-חדר-מלחמה)
9. [דוח סיכום לילה](#9-דוח-סיכום-לילה)
10. [טיפים ושאלות נפוצות](#10-טיפים-ושאלות-נפוצות)

---

## 1. מבוא ותפקידים

### מה המערכת עושה

**Night-Ops System** (DeployCenter) מנהל תהליך שחרור גרסת תוכנה מקצה לקצה:

```
שלב 1: איסוף משימות (ראשי צוות מגישים CR-ים)
    ↓
שלב 2: בניית תוכנית (מנהל לילה מסדר, מתזמן, מגדיר תלויות)
    ↓
שלב 3: סקירה ואישור (בדיקה סופית לפני ביצוע)
    ↓
שלב 4: חזרה גנרלית (סימולציה של הלילה)
    ↓
שלב 5: ליל ההטמעה (ביצוע בזמן אמת)
    ↓
שלב 6: בוקר שלמחרת + סיכום
```

### תפקידים במערכת

| תפקיד | שם במערכת | הרשאות עיקריות |
|-------|-----------|----------------|
| **מנהל מערכת** | `ADMIN` | כל ההרשאות, ניהול משתמשים |
| **מנהל לילה** | `RELEASE_MANAGER` | ניהול גרסה מלא, GO/NO-GO |
| **ראש צוות** | `TEAM_LEAD` | הגשת משימות, ניהול צוות |
| **עובד** | `EMPLOYEE` | צפייה וביצוע משימות |
| **צופה** | `VIEWER` | צפייה בלבד |

---

## 2. כניסה למערכת

### כניסה ראשונה

1. פתח את הדפדפן בכתובת שסיפק לך מנהל המערכת
2. הכנס **כתובת מייל** וסיסמה
3. לחץ **התחבר**

> המערכת לא תנעל חשבון לאחר ניסיונות כושלים, אך מנהל המערכת יכול להשבית חשבון.

### שינוי סיסמה

פנה למנהל המערכת לאיפוס סיסמה — שינוי סיסמה עצמאי ייתמך בגרסאות הבאות.

### לאחר הכניסה

**מנהל לילה / ADMIN** → מסך ראשי עם לשוניות:
- **גרסאות** — רשימת כל הגרסאות
- **צוותים** — ניהול צוותים
- **ייבוא** — ייבוא נתונים מExcel
- **סיכום** — דוחות סיכום
- **War Room** — חדר מלחמה (בזמן הלילה)

**ראש צוות** → מסך "ממשק ראש צוות" — הגשת משימות ועדכון סטטוס

**עובד** → לוח מחוונים עם משימות הצוות

---

## 3. מדריך מנהל לילה

### 3.1 יצירת גרסה חדשה

**נתיב:** לשונית "גרסאות" → לחץ **"+ גרסה חדשה"**

**שדות חובה:**
- **שם גרסה** — לדוגמה: `ITv05-2026`
- **שעת התחלה מתוכננת** — שעת תחילת ליל ההטמעה
- **שעת סיום מתוכננת** — שעה שבה צפוי כל הלילה להסתיים

**שדות אופציונליים:**
- **תיאור** — פרטים נוספים על הגרסה
- **מועד סגירת איסוף** — deadline להגשת CR-ים
- **שעת ישיבת סקירה** — מועד הישיבה לפני הלילה
- **מזהה QC Release** — לקישור עם מערכת QC

לאחר יצירה, הגרסה נמצאת בסטטוס **DRAFT**.

---

### 3.2 הגדרת שלבים (Phases)

**נתיב:** בחר גרסה → לחץ **"ערוך שלבים"** / **"אתחל שלבים"**

#### אתחול אוטומטי

לחץ **"אתחל שלבים ברירת מחדל"** ליצירה מהירה של:
- שלב 1: HOT (Pre-Deploy)
- שלב 2: HOT (Deploy)
- שלב 3: HOTNET (Deploy)
- שלב 4: בוקר שלמחרת

#### הגדרה ידנית

לחץ **"+ שלב"** והגדר:
- **שם השלב**
- **סביבה** (HOT / HOTNET / COLD / BOTH)
- **isGoNoGo** — סמן על השלב האחרון לפני פעילויות הבוקר

> **חשוב:** שלב שמסומן isGoNoGo הוא נקודת ההחלטה GO/NO-GO. משימות בשלבים **אחריו** לא יחסמו את אישור הסיכום.

#### הוספת תת-שלבים (Sub-Phases)

בתוך כל שלב, הוסף תת-שלבים לארגון המשימות:
1. לחץ **"+ תת-שלב"** בתוך שלב
2. הזן שם (לדוגמה: "העלאת BEs", "בדיקות עשן")
3. קבע סדר (`orderIndex`)

---

### 3.3 שלב איסוף משימות (COLLECTING)

**הפעלה:** שנה סטטוס גרסה → **COLLECTING**

בשלב זה:
- ראשי צוות יכולים לגשת ולהגיש משימות
- הממשק של ראשי הצוות מוצג

#### מעקב הגשות

בדף הגרסה, בלשונית **"הגשות"** תראה:

| צוות | סטטוס |
|------|-------|
| צוות Backend | ✅ הוגש |
| צוות DB | ⏳ ממתין |
| צוות Frontend | ❌ לא התחיל |

> **רק צוותים עם CR-ים פעילים** מוצגים כ"נדרשים להגשה". צוותים ללא CR לא חוסמים את ההתקדמות.

#### סגירת איסוף

לחץ **"קדם לשלב הבא"** (מ-COLLECTING ל-REFINING).

אם צוות עם CR טרם הגיש, תוצג הודעה:
```
הצוותים הבאים טרם הגישו: צוות Backend, צוות DB
```

ניתן לאשר ולהמשיך בכל זאת (אחריות המנהל).

---

### 3.4 שלב עיבוד (REFINING)

בשלב זה המנהל בונה את תוכנית הביצוע:

#### ייבוא משימות מExcel

**נתיב:** ייבוא → בחר קובץ Excel

פורמט הקובץ הנדרש (עמודות):

| CR Number | Title | Team | Environment | Priority | Description | Duration |
|-----------|-------|------|-------------|----------|-------------|----------|

לאחר ייבוא, המשימות מופיעות בתוכנית.

#### עריכת משימות

לחץ על כל משימה לעריכה:
- **כותרת / תיאור / הערות**
- **צוות מבצע**
- **עובד אחראי**
- **שעת ביצוע מתוכננת** (plannedStart / plannedEnd)
- **עדיפות** (LOW / MEDIUM / HIGH / CRITICAL)
- **סביבה** (HOT / HOTNET / COLD)

#### תזמון אוטומטי

לחץ **"תזמן מחדש"** כדי לחשב שעות ביצוע אוטומטית:
1. הגדר **שעת התחלה לכל שלב**
2. המערכת מחשבת שעות אוטומטית על-פי משך המשימות
3. אפשרות: **"שמור על תלויות"** — משימה לא תתחיל לפני שהקודמת לה מסתיימת

#### ניהול תלויות

**הוספת תלות:**
1. פתח משימה
2. לחץ **"+ תלות"**
3. בחר את המשימה שצריכה לסיים קודם
4. המשימה תעבור אוטומטית לסטטוס WAITING עד שהתלות מסתיימת

**יצירה אוטומטית:**
- **"תלויות לפי משתמש"** — משימות של אותו עובד בשלבים שונים יקבלו תלות אוטומטית
- **"פתור תלויות"** — המשימות WAITING שתלויותיהן הושלמו יעברו ל-OPEN

#### שינוי שיוך עובדים

**"שינוי עובד במשימות"** → בחר עובד נוכחי ועובד חלופי → החלפה מרוכזת.

---

### 3.5 שלב סקירה ואישור

**REFINING → REVIEW → APPROVED**

בשלב זה:
- בצע ישיבת סקירה עם ראשי הצוותות
- וודא שכל המשימות שובצו ותוזמנו
- לחץ **"אשר גרסה"** לעבור ל-APPROVED

---

### 3.6 חזרה גנרלית (REHEARSAL)

**APPROVED → REHEARSAL**

החזרה הגנרלית היא סימולציה מלאה של ליל ההטמעה:

1. המשימות מאופסות ל-OPEN
2. הצוותות מבצעים את המשימות כאילו זה הלילה האמיתי
3. בסיום, לחץ **"סיים חזרה גנרלית"**

**אישור סיכום החזרה:**
- כל המשימות בשלבים עד isGoNoGo חייבות להיות בסטטוס DONE/FAILED/ROLLED_BACK
- לאחר אישור: המשימות מאופסות, הגרסה חוזרת ל-APPROVED
- ניתן להריץ חזרות מרובות

---

### 3.7 ליל ההטמעה (ACTIVE)

**APPROVED → ACTIVE**

**פעולות בתחילת הלילה:**
1. עבור ל**חדר המלחמה (War Room)**
2. פקח על התקדמות בזמן אמת
3. עדכן סטטוסי משימות שעדיין פתוחות

**GO/NO-GO:**
בסיום שלב הGO-NO-GO (השלב המסומן):
- המערכת מחשבת האם כל המשימות הסתיימו
- מוצג לחצן **GO** (ירוק) או **NO GO** (אדום)
- ניתן לעקוף ולאשר למרות NO-GO (אחריות המנהל)

---

### 3.8 בוקר שלמחרת (MORNING_AFTER)

**ACTIVE → MORNING_AFTER**

המעבר לשלב הבוקר מאפשר:
- ביצוע פעילויות לאחר ההטמעה
- בדיקות smoke tests
- הפקת דוח סיכום

---

### 3.9 סיכום (COMPLETED)

**נתיב:** לשונית "סיכום" → "סיכום ליל ההטמעה"

**מה הדוח כולל:**
- אחוז השלמת משימות
- לוחות זמנים בפועל מול תכנון
- רשימת תקלות
- CRים שנפרסו
- נתוני QC (כשמחוברים)
- הערות לצוות הבוקר

**אישור סיכום:**
1. קרא את הדוח
2. סמן "קראתי ואישרתי"
3. לחץ **"אשר סיכום"**
4. הגרסה עוברת ל-COMPLETED ✅

---

## 4. מדריך ראש צוות

### 4.1 הגשת משימות לגרסה

**מתי:** כאשר הגרסה בסטטוס COLLECTING

**נתיב:** לשונית "הגשת משימות" / ממשק ראש צוות

#### הוספת CR חדש

1. לחץ **"+ CR חדש"**
2. מלא את הפרטים:

| שדה | הסבר | חובה |
|-----|-------|------|
| **מספר CR** | מזהה ב-Jira/CRM | כן |
| **כותרת** | תיאור קצר | כן |
| **סביבה** | HOT / HOTNET / COLD / BOTH | כן |
| **תיאור** | פרטים מלאים | לא |
| **תוכנית Rollback** | מה לעשות אם נכשל | לא |
| **הערות** | כל מידע נוסף | לא |

#### הוספת משימות תשתית

לפעילויות שאינן CR (בדיקות, הכנות):
1. לחץ **"+ משימה חופשית"**
2. הזן כותרת ותיאור
3. **ללא CR number**

#### סיום הגשה

לאחר שהכנסת את כל המשימות:
1. לחץ **"סיימתי הגשה"**
2. מוצגת הודעת אישור ✅
3. המנהל רואה את הצוות שלך כ"הוגש"

> **לא ניתן לשנות** לאחר לחיצה על "סיימתי". פנה למנהל הלילה אם נדרש שינוי.

---

### 4.2 ניהול משימות בזמן הלילה

**נתיב:** לשונית "צוות שלי" או לוח המחוונים

#### עדכון סטטוס משימה

לחץ על משימה ובחר סטטוס:

| סטטוס | משמעות | צבע |
|-------|---------|-----|
| **OPEN** | ממתינה לביצוע | כחול |
| **IN_PROGRESS** | בביצוע כעת | כתום |
| **DONE** | הושלמה בהצלחה | ירוק |
| **FAILED** | נכשלה | אדום |
| **ROLLED_BACK** | בוצע Rollback | סגול |
| **BLOCKED** | חסומה — מצריכה עזרה | אדום כהה |
| **WAITING** | ממתינה לתלות אחרת | אפור |

#### דיווח על חסימה

כאשר משימה **BLOCKED**:
1. שנה סטטוס ל-BLOCKED
2. הוסף **סיבת חסימה** (חובה!)
3. התראה נשלחת אוטומטית לחדר המלחמה

#### מעקב תלויות

אם המשימה שלך בסטטוס **WAITING** — היא תיפתח אוטומטית ל-OPEN כאשר המשימה הקודמת בתלות תסתיים.

---

### 4.3 הגשת הצוות לתוכנית (REFINING)

כאשר מנהל הלילה מסיים לבנות את התוכנית, ניתן:
- לצפות בתוכנית הצוות שלך
- לעדכן הערות על משימות
- לא ניתן לשנות CRים לאחר מעבר מ-COLLECTING

---

## 5. מדריך עובד

### 5.1 לוח המחוונים

לאחר כניסה, מוצג **לוח מחוונים** עם:
- **המשימות שלי** — משימות שהוקצו לי אישית
- **משימות הצוות** — כל משימות הצוות שלי
- **מהירות** — סיכום סטטוסים

### 5.2 עדכון סטטוס משימה

1. מצא את המשימה שלך
2. לחץ על שם המשימה לפתיחת פרטים
3. בחר סטטוס חדש מהתפריט
4. לחץ **"עדכן"**

**כללים:**
- תוכל לעדכן רק משימות שהוקצו לצוות שלך
- IN_PROGRESS → DONE: המערכת מתעדת שעת סיום
- אם תשנה ל-BLOCKED: חייב להוסיף סיבה

### 5.3 צפייה בתוכנית הכללית

כאשר הגרסה ב-ACTIVE, ניתן לצפות בתוכנית הכללית אבל **לא לשנות** אותה.

### 5.4 הערות ותיעוד

עבור כל משימה, ניתן להוסיף:
- **הערות** — הסברים על מה שנעשה
- **מעקב בוקר** — סמן אם המשימה מצריכה בדיקה בבוקר שלמחרת

---

## 6. מדריך מנהל מערכת

### 6.1 ניהול משתמשים

**נתיב:** לשונית "ניהול" → "משתמשים"

#### הוספת משתמש

1. לחץ **"+ משתמש חדש"**
2. מלא:
   - **שם מלא**
   - **מייל** (שם משתמש לכניסה)
   - **תפקיד**
   - **סיסמה זמנית**
3. לחץ **"שמור"**

#### עדכון תפקיד

1. מצא משתמש ברשימה
2. לחץ **"עריכה"**
3. שנה תפקיד
4. לחץ **"שמור"**

#### ניהול חברות בצוות

1. עבור ל**"ניהול צוותים"**
2. לחץ על צוות
3. לחץ **"+ הוסף חבר"**
4. בחר משתמש וסמן האם **ראש צוות**

#### איפוס סיסמה

כרגע: עדכן ישירות בבסיס הנתונים:
```bash
cd backend
npx ts-node -e "
const { PrismaClient } = require('@prisma/client');
const bcrypt = require('bcryptjs');
const prisma = new PrismaClient();
async function main() {
  const hash = await bcrypt.hash('NewPass123!', 10);
  await prisma.user.update({
    where: { email: 'user@company.com' },
    data: { password: hash },
  });
  console.log('Password reset');
  await prisma.\$disconnect();
}
main().catch(console.error);
"
```

---

### 6.2 ניהול צוותים

**נתיב:** לשונית "צוותים"

#### יצירת צוות

1. לחץ **"+ צוות חדש"**
2. הגדר:
   - **שם הצוות**
   - **קוד מערכת** (לדוגמה: `BACKEND-01`)
3. לחץ **"צור"**

#### ניהול חברי צוות

1. פתח צוות
2. לחץ **"+ הוסף חבר"**
3. בחר משתמש מהרשימה
4. סמן **"ראש צוות"** אם רלוונטי

---

### 6.3 ייבוא נתונים

**נתיב:** לשונית "ייבוא"

#### ייבוא גרסה מExcel

קובץ Excel עם גיליון "Tasks" ועמודות:

| שדה | תיאור |
|-----|-------|
| CRNumber | מספר CR |
| Title | כותרת המשימה |
| Team | שם הצוות |
| Environment | HOT / HOTNET / COLD |
| Priority | LOW / MEDIUM / HIGH / CRITICAL |
| Description | תיאור |
| Duration | משך בדקות |
| AssignedUser | שם עובד מבצע |

**תהליך ייבוא:**
1. בחר קובץ Excel
2. לחץ **"ייבא"**
3. בדוק תוצאות — שגיאות שורה מוצגות
4. משימות שיובאו מוצגות בתוכנית

---

## 7. מחזור חיים של גרסה — מדריך מלא

### תרשים מצבים

```
DRAFT → COLLECTING → REFINING → REVIEW → APPROVED → REHEARSAL (אופציונלי)
                                                  ↓
                                               ACTIVE → MORNING_AFTER → COMPLETED
                                                  ↓
                                           ROLLED_BACK (במקרה חירום)
```

### מטריצת פעולות לפי שלב

| פעולה | DRAFT | COLLECTING | REFINING | REVIEW | APPROVED | ACTIVE |
|-------|-------|------------|---------|--------|----------|--------|
| עריכת פרטי משימה | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ נעול |
| הגשת CR (ראש צוות) | ❌ | ✅ | ❌ | ❌ | ❌ | ❌ |
| עדכון סטטוס משימה | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |
| תזמון אוטומטי | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ |
| War Room | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |
| הפקת סיכום | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |

---

## 8. חדר מלחמה

**נתיב:** לשונית "War Room" (זמין רק כש-ACTIVE)

### מה מוצג

**תצוגה כללית:**
- **Progress Bar** — אחוז השלמה הכולל
- **כרטיסיות סטטוס** — כמה משימות בכל סטטוס
- **התראות** — משימות שנחסמו (אדום) / משימות מתעכבות (כתום)

**טבלת משימות:**
- כל המשימות בגרסה
- סטטוס, שלב, צוות, עובד, תזמון
- סינון לפי: סטטוס / שלב / צוות / עובד

**עמודת GO/NO-GO:**
- ✅ ירוק: אפשר להמשיך לשלב הבא
- 🔴 אדום: יש עדיין משימות פתוחות

### פעולות בWar Room

**שינוי סטטוס מרוכז:**
- סמן מספר משימות
- לחץ **"שנה סטטוס"**
- בחר סטטוס חדש

**סינון מהיר:**
- `סטטוס = BLOCKED` → הצג רק חסומות
- `צוות = X` → הצג משימות של צוות X

**עדכון הערות:**
- לחץ על משימה → הוסף הערות מהירות

---

## 9. דוח סיכום לילה

**נתיב:** לשונית "סיכום"

### ממשק הסיכום

**Banner GO/NO-GO:**
- ✅ GO — כל המשימות הושלמו (עד שלב isGoNoGo)
- 🛑 NO GO — עדיין יש משימות פתוחות

**לוחות זמנים:**
- השוואה בין מתוכנן ובפועל לכל שלב
- חריגות מוצגות באדום עם שדה חובה לסיבה

**תקלות:**
- משימות BLOCKED / FAILED
- שדה להסבר לכל תקלה

**QC (כאשר מחוברים):**
- טבלת Test Coverage
- רשימת Defects

**עיקרי הדברים:**
- מלל חופשי לסיכום הלילה

### אישור הסיכום

1. קרא את הדוח בעיון
2. הוסף עיקרי הדברים אם נדרש
3. הוסף הערות לצוות הבוקר
4. סמן **"קראתי ואישרתי שכל הנתונים נכונים"**
5. לחץ **"אשר סיכום"**

> **תנאי:** כל המשימות בשלבים עד isGoNoGo חייבות להיות בסטטוס סופי (DONE / FAILED / ROLLED_BACK) לפני שניתן לאשר. מנהל לילה יכול לעקוף ולאשר בכל מקרה.

### ייצוא לWord

לאחר אישור, לחץ **"הורד כWord"** לקבלת דוח מעוצב.

### שליחה למייל

לחץ **"העתק לאימייל"** לקבלת טקסט מעוצב להדבקה בתוכנת מייל.

---

## 10. טיפים ושאלות נפוצות

### שאלות נפוצות

**ש: הלחצן "קדם לשלב הבא" אפור — מדוע?**
ת: ייתכן שצוות עם CR לא השלים הגשה. בדוק לשונית "הגשות" לראות מי חסר.

**ש: משימה תקועה על WAITING — מה עושים?**
ת: הסטטוס WAITING אומר שהמשימה מחכה לתלות. לחץ על המשימה ובדוק "תלויות" — מה עדיין לא הושלם.

**ש: הסיכום מראה NO-GO אבל כל המשימות הושלמו — מה קורה?**
ת: יש תלויות שמשימות מוגדרות עליהן עדיין ב-WAITING. השתמש ב"פתור תלויות" כדי לעדכן.

**ש: עשיתי טעות בעדכון סטטוס — אפשר לשנות?**
ת: כן, כל עוד הגרסה לא COMPLETED ניתן לשנות בחזרה.

**ש: לא רואה את הגרסה הפעילה — מה קורה?**
ת: וודא שמשתמשך מוגדר כחבר בצוות שמשויך לגרסה, ושהגרסה לא בסטטוס DRAFT.

**ש: הVault Room לא מוצג — מה קורה?**
ת: War Room מוצג רק כאשר הגרסה בסטטוס ACTIVE.

---

### טיפים לשימוש יעיל

1. **בנה שלבים לפני שמתחיל האיסוף** — ראשי הצוות יוכלו לבחור סביבה מתוך רשימה
2. **השתמש בתזמון אוטומטי** כנקודת התחלה — ואז כוונן ידנית
3. **הגדר isGoNoGo נכון** — שגיאה כאן תשנה את נקודת ההחלטה GO/NO-GO
4. **חזרה גנרלית חובה** לגרסאות גדולות — מאפשרת לתפוס בעיות לוגיסטיות
5. **הוסף תלויות בין משימות** כדי שמשימות לא יתחילו לפני הזמן
6. **War Room** — השאר פתוח לכל הלילה על מסך ייעודי

---

### קיצורי מקלדת ופעולות מהירות

| פעולה | איך |
|-------|-----|
| פתיחת פרטי משימה | לחץ על שורת המשימה |
| שינוי סטטוס מהיר | לחץ על Badge הסטטוס |
| ריענון נתונים | F5 / רענון הדף |
| חיפוש משימה | Ctrl+F בדפדפן |

---

*Night-Ops System — הופק אוטומטית, מאי 2026*
